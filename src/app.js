import React from 'react';
import World from './Features';

const app = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <World />
    </div>
  );
};

export default app;
